import React from 'react';

const Favorite = (props) => {
    
    
    return(
        <div>
            <p>{props.title}</p>
            <p>{props.artist}</p>
            <p>{props.time}</p>
        </div>
    ) 
}